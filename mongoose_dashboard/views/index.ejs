<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" type="text/css" href="/reset.css">
    <link rel="stylesheet" type="text/css" href="/style.css">
    <title>Mongoose Dashboard</title>
</head>
<body>
    <div id='wrapper'>
        <div class='messages'>
                <% JSON.stringify(messages) %>
                <% if(messages.quote) { %>
                    <% for (var x of messages.quote) { %>
                        <h3><%= x %></h3>
                    <% } %>
                <% } %>
        </div>
        <div class="addbtn">
            <form class='new' action='/new' method='get'>
                <input class='newbtn' action='/new' type='submit' value='Add a new mongoose!'>
            </form>   
        </div>
        <div id='results'>
            <h1>Here are the mongooses we currently have!</h1>
            <% for (var x in musers) { %>
                <h3>"<%= musers[x].name %>"</h3>
                <a href='/display/<%= musers[x]._id %>'><button>Details</button></a>
                <a href='/edit/<%= musers[x]._id %>'><button>Update</button></a>
                <a href='/destroy/<%= musers[x]._id %>'><button>Destroy</button></a>
                <hr>
            <% } %>
        </div>
    </div>
</body>
</html>